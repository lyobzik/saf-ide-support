{
  "main": {
    "type": "form_filling",
    "form": "hello_form",
    "answer": "{{ main_form.name }}",
    "unknown": "{{ main_form.age }}",
    "chain": "{{ main_form.name.extra }}",
    "nested": "{{ variables.main_form.name }}",
    "statement": "{% set x = main_form.age %}",
    "bare": "{{ main_form }}"
  }
}
