package ru.sber.smartapp.dsl.index

/**
 * Значение индекса для ключа определения: смещения токенов *имён*
 * верхнеуровневых свойств, определяющих сущность под одним составным ключом в
 * одном файле.
 *
 * Нужен именно список (а не скаляр), потому что [com.intellij.util.indexing.DataIndexer]
 * возвращает одно значение на ключ для файла; два определения с одним именем в
 * *одном* файле должны оба сохраниться (скаляр перетёрся бы). Формат хранения —
 * см. [SmartAppDefinitionExternalizer].
 */
data class SmartAppDefinitionValue(val offsets: List<Int>)
